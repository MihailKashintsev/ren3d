# Ren3D

> 🇬🇧 [English version — README.md](README.md)

Локальный офлайн 3D-редактор на базе Electron + Three.js.  
Создавай и редактируй 3D-модели из примитивов, экспортируй в STL/OBJ.

![Platform](https://img.shields.io/badge/платформа-Windows%20%7C%20Linux-blue)
![Electron](https://img.shields.io/badge/Electron-29-47848F)
![Three.js](https://img.shields.io/badge/Three.js-r128-black)
![License](https://img.shields.io/badge/лицензия-MIT-green)

---

## Возможности

- Примитивы: куб, сфера, цилиндр, конус, тор, плоскость
- Визуальное гизмо трансформаций (стрелки перемещения / вращения / масштаба)
- Булевы операции: объединение, вычитание, пересечение
- Материалы: цвет, металлик, шероховатость, прозрачность
- Экспорт: STL, OBJ — Импорт: STL, OBJ
- Отмена / Повтор (50 шагов)
- Тёмный glassmorphism интерфейс
- Работает полностью офлайн после установки

---

## Установка на Linux

**Одна команда (рекомендуется):**
```bash
bash <(curl -sL https://raw.githubusercontent.com/MihailKashintsev/ren3d/master/installer/ren3d-install.sh)
```

**Или с USB-флешки:**
```bash
bash ren3d-install.sh
```

**Обновление до последней версии:**
```bash
bash ren3d-install.sh
# выбери пункт 1 — Обновить приложение (быстро, скачает только новый index.html)
```

Работает на: Ubuntu, Debian, Fedora, Arch, Mint, Pop!\_OS и любом дистрибутиве.  
`sudo` не требуется — устанавливается в `~/.local/`.

---

## Установка на Windows

1. Установи [Node.js LTS](https://nodejs.org)
2. Скачай или клонируй репозиторий
3. Открой папку `scripts/`, двойной клик на **`build.bat`**
4. Выбери пункт `1` — Windows installer
5. Запусти `.exe` из папки `app/dist/`

---

## Горячие клавиши

| Действие | Клавиши |
|---|---|
| Добавить куб / сферу / цилиндр | `Shift+C` / `Shift+S` / `Shift+Y` |
| Добавить конус / тор / плоскость | `Shift+O` / `Shift+T` / `Shift+P` |
| Инструмент перемещение / вращение / масштаб | `G` / `R` / `S` |
| Инструмент выбора | `Q` |
| Дублировать | `Ctrl+D` |
| Удалить | `Del` |
| Отмена / Повтор | `Ctrl+Z` / `Ctrl+Y` |
| Выделить всё | `Ctrl+A` |
| Фокус на объекте | `F` |
| Показать всё | `Home` |
| Сетка | `Z` |
| Экспорт STL | `Ctrl+E` |
| Импорт | `Ctrl+I` |

---

## Режим разработки

```bash
cd app
npm install
npm run dev
```

---

## Структура проекта

```
ren3d/
├── app/                             # Electron-приложение
│   ├── src/
│   │   └── index.html               # Весь Ren3D (Three.js)
│   ├── main.js                      # Главный процесс Electron
│   ├── preload.js                   # Безопасный IPC-мост
│   ├── package.json
│   ├── electron-builder.config.js   # Профили сборки Win/Linux
│   └── assets/                      # Иконки
│
├── installer/
│   └── ren3d-install.sh             # Linux-установщик (всегда скачивает последнее)
│
├── scripts/
│   ├── build.bat                    # Сборка для Windows (двойной клик)
│   └── build.sh                     # Сборка для Linux/WSL
│
├── .github/workflows/
│   └── build.yml                    # Автосборка при каждом push
│
├── README.md                        # Документация на английском
└── README.ru.md                     # Документация на русском
```

---

## GitHub Actions (автосборка)

Каждый push в `master` затрагивающий файлы в `app/` автоматически:
- Собирает **Windows** `.exe` (установщик + portable)
- Собирает **Linux** `.AppImage` + `.deb`
- Загружает как артефакты (вкладка Actions в репозитории)

**Создать релиз** — запушить тег версии:
```bash
git tag v2.1.0
git push origin v2.1.0
```
Это создаст GitHub Release со всеми бинарниками.

---

## Как работает обновление

```
Ты пушишь изменения в app/src/index.html
          ↓
GitHub Actions автоматически собирает новые бинарники
          ↓
Пользователь запускает: bash ren3d-install.sh → пункт 1
          ↓
Скачивается только новый index.html с GitHub (~80 КБ, секунды)
          ↓
Готово — установлена новая версия
```

---

## Лицензия

MIT
